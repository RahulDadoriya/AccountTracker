<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.accounttrack"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="example.accounttrack.HomePage"
            android:label="@string/home" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".TransferAmount"
            android:label="@string/app_name"
            />
        <activity
            android:name=".CreateAccount"
            android:label="@string/app_CreateAccount"
            />
        <activity
            android:name=".Transaction"
            android:label="@string/app_trans"
            />
        <activity
            android:name=".TransactionDetails"
            android:label="@string/app_transd"
            />
    </application>

</manifest>